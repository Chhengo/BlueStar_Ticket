RateLimiterService.java 你已经有完整代码了，不是从零写，是读懂它。
先给自己2分钟，回答我这一个问题：
isAllowed("grab:event:1", 100, 200) 这行调用， 100 和 200 分别控制什么？ 如果我把 100 改成 10 会发生什么？

isAllowed("grab:event:1", 100, 200) 这行调用， 100 和 200 分别控制什么？ 如果我把 100 改成 10 会发生什么？

100是每秒放令牌的数量 200是桶大小 如果当前在一秒内 1s开始前拿了100个 桶里还有100个
在令牌桶即将更新为200时会不会又出现拿走100个的情况 如果100改成10 1s只能放10个令牌 那qps会大大下降
 影响用户使用 你说的isAllowed("grab:event:1", 100, 200) 也就是抢票时该活动下允许通过的100个请求 我的理解是这样的

答对了核心，但有一个地方想歪了，帮你纠正：
"在令牌桶即将更新为200时会不会又出现拿走100个的情况"
令牌桶不是定时刷新的，不存在"即将更新为200"这个时刻。
它是连续补充的：每过1毫秒就补 100/1000 = 0.1 个令牌，是平滑的，不是攒够了再一次性给。
所以不会出现你担心的"瞬间涌入200个请求"的情况。
这就是令牌桶比"计数器限流"好的地方——平滑，不会产生临界点突刺。

这个 Service 里 execute() 方法传了6个东西进去：
rateLimitScript, keys, rate, capacity, now, "1"
为什么 rate/capacity/now 要用 String.valueOf() 包一下传进去？ 直接传 int/long 不行吗？

rateLimitScript, keys, rate, capacity, now, "1"
这些参数是lua脚本里需要运行的内容 实现实际的令牌桶限流算法 可以放令牌 桶大小以及当前时间都需要转String
我理解是因为redis通过lua实现 lua的key只支持string并输出json的格式 以redisconfig为例
我认为转格式是为了防止执行后输出的什么内容不会变为乱码 但是我不清楚是什么 我的大概思路就是这样

RedisConfig 里把 value 序列化器设成了 StringRedisSerializer。
这意味着 Redis 存进去、取出来的值都是字符串。
Lua 脚本里用 tonumber(ARGV[1]) 来解析数字——tonumber 只能把 "100" 转成 100，
如果传进去的是 Java 序列化后的二进制字节（默认 JdkSerializationRedisSerializer）
tonumber 拿到的是乱码，返回 nil，然后整个算法就崩了。
所以 String.valueOf() 不是"防乱码"，是保证 Lua 能用 tonumber() 正确解析。
一句话记住：
Redis 值序列化用 String → 传参也必须是 String → Lua 里 tonumber() 才能工作

下一个技术点是扣库存 Lua 脚本，这个和令牌桶有相似结构。
先说思路：
扣库存需要做哪几步？Redis 里要判断什么，再做什么？
用一句话或伪代码写出来。

用户视角 用户点击活动详情 选择票档 购买指定票
后台 预缓存总库存 判断是否为限流后的请求 是否持有令牌 是 则用lua脚本库存-1 并且当前user只能有一张票（暂定规则）
lua实现 获取用户持有的key 一个key对应一张票 不超卖 思路是这样 具体实现不知道

你漏掉的一步：扣之前要先判断库存够不够
你说的是"库存-1"，但如果库存已经是0了还-1，就变成-1了，这就是超卖。
所以 Lua 脚本的逻辑应该是：
1. GET 当前库存
2. 如果库存 <= 0 → 返回0（拒绝，已售罄）
3. 如果库存 > 0 → DECR 库存，返回1（允许）
为什么必须用 Lua？ 因为 GET 和 DECR 是两步，并发下两个线程都 GET 到库存=1，都认为可以扣，就都执行 DECR，库存变-1，超卖了。
还有你说的"一个user只能有一张票"——这个不适合放在 Lua 扣库存脚本里。
扣库存 和 校验用户是否已购 是同一件事吗？分别在哪一层做更合适？

扣减库存是stock减一 也就是用户请求付款之前已经实现 我这里不知道在那一层
但是校验是订单已经创建好了 是order层面 后续用kafka异步下单需要校验这一步

完全正确，说得很清楚：
扣库存 → 抢票那一刻（stock层，Redis）
校验重复购买 → 落单那一刻（order层，Kafka消费者里）
这就是这个系统的核心分层，记住这个：
请求进来
  → 限流（令牌桶）
  → 扣库存（Lua，Redis）      ← 这一层只管"票够不够"
  → 发Kafka消息
      → 消费者落单（校验user是否已购，幂等）  ← 这一层管"这人能不能买"

现在你来回答一个问题，然后我们进入 /orders/grab 接口：
deductStock 返回 false 时，接口应该给前端返回什么？
返回 true 之后，下一步是什么？（结合你的分层图想）


/orders/grab 接口：
deductStock 返回false时 接口应该给前端返回什么？ 返回 true 之后，下一步是什么？（结合你的分层图想）
错误就代表着 没抢到 返回
票已经抢光 true 抢到之后先创建订单
把当前用户id name 活动id
票id全部录入到订单接口准备创建订单
再组装完成后准备发给kafka生产者
之后再进行消费 异步落库

false → 返回"票已售罄"
true  → 组装订单消息 → 发Kafka → 异步消费落库
直接给你 /orders/grab 接口实现：

现在有一个问题给你，直接关系到明天的 Kafka 消费者实现：
orderNo 是在生产者这里生成的，不是在消费者里生成的。
为什么？如果放在消费者里生成会出现什么问题？